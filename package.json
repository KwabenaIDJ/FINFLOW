{
  "name": "finflow-dashboard",
  "version": "1.0.0",
  "description": "FinFlow Premium Financial Dashboard mobile app",
  "main": "index.html",
  "scripts": {
    "build": "node copy-assets.js",
    "cap:add-android": "npx cap add android",
    "cap:sync": "npm run build && npx cap sync",
    "cap:open-android": "npm run build && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.1",
    "@capacitor/core": "^6.1.1",
    "@capacitor/local-notifications": "^6.1.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.1"
  }
}
